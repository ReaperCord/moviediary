spring:
  application:
    name: moviediary

  datasource:
    url: jdbc:postgresql://localhost:5431/moviediary
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  flyway:
    enabled: true